jobs:
  say-hello:
    docker:
      - image: cimg/base:current
    parallelism: 3
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "python3 main.py"
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
